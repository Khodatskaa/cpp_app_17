#include "boiler.h"

Boiler::Boiler(std::string firm, std::string color, double power, double volume, double heatingTemperature)
    : firm(firm), color(color), power(power), volume(volume), heatingTemperature(heatingTemperature) {
    
}
